apiVersion: apps/v1
kind: Deployment
metadata:
  name: disease-v2
spec:
  replicas: 1
  selector:
    matchLabels:
      app: disease-v2
  template:
    metadata:
      labels:
        app: disease-v2
    spec:
      containers:
      - name: disease
        image: santhoshh005/disease:v2
        ports:
        - containerPort: 3000
---
apiVersion: v1
kind: Service
metadata:
  name: disease-v2-service
spec:
  selector:
    app: disease-v2
  ports:
    - port: 80
      targetPort: 3000
  type: NodePort